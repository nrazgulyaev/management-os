# Good patterns observed

64 page(s) shipped clean (USABLE verdict, no stage labels, no dev leaks, no next-wave badges).

A representative sample:

- `/dashboard/bookings` — h1=true table=true
- `/dashboard/channels` — h1=true table=true
- `/dashboard/direct-bookings/deposits` — h1=true table=true
- `/dashboard/direct-bookings/guest-status` — h1=true table=true
- `/dashboard/direct-bookings/holds` — h1=true table=true
- `/dashboard/direct-bookings/reconciliation` — h1=true table=true
- `/dashboard/direct-bookings/requests` — h1=true table=true
- `/dashboard/documents` — add=1
- `/dashboard/finance` — h1=true table=true
- `/dashboard/guest-journey/rules` — add=1
- `/dashboard/guest-journey/suggestions` — h1=true table=true
- `/dashboard/guest-stays/tokens` — h1=true table=true
- `/dashboard/guests` — h1=true table=true
- `/dashboard/integrations` — h1=true table=false
- `/dashboard/integrations/calendar-events` — add=1
- `/dashboard/integrations/calendar-feeds` — add=1
- `/dashboard/integrations/conflicts` — h1=true table=true
- `/dashboard/inventory` — add=1
- `/dashboard/inventory/categories` — h1=true table=true
- `/dashboard/inventory/items` — add=1

These are the templates to copy when fixing the noisier pages.
