# Empty-state quality

Pages with empty-state copy but no Add CTA:

- `/dashboard` — "Three arrivals today, all on track except EV-07, whose supervisor review has not yet cleared — clean"
- `/dashboard/billing/upgrade` — "14-day trial with limited cabinets and no integrations."
- `/dashboard/direct-bookings` — "Quote → temporary inventory hold → guest contact form → manual concierge approval → canonical bookin"
- `/dashboard/front-office/arrivals` — "No arrivals scheduled."
- `/dashboard/front-office/departures` — "Bookings due to check out. Cleaning task status, late-checkout request status, and the same-day next"
- `/dashboard/front-office/in-house` — "No in-house stays."
- `/dashboard/guest-journey` — "Deterministic timed rules across pre-arrival → post-stay. Suggestions are CTAs (no purchases); revie"
- `/dashboard/guest-journey/runs` — "Each row is one (booking, rule) pair. Idempotent — re-runs are no-ops on the unique index."
- `/dashboard/guest-stays` — "Issue signed stay tokens, edit the villa guide, manage the smart-lock stub. The guest portal at /sta"
- `/dashboard/inventory/counts` — "No counts on record yet."
- `/dashboard/notifications/inbox` — "In-app notifications addressed to you. Email / SMS / WhatsApp ship through Resend / Twilio when thos"
- `/dashboard/operations/checklists` — "Standard turnover cleaning checklist used between guest stays."
- `/dashboard/owner-intelligence/calendar` — "No owner-linked villas found."
- `/dashboard/owner-intelligence/preferences` — "Each owner can decide how the owner portal displays their calendar — guest names, country, channel l"
- `/dashboard/owner-stays` — "Owner-portal requests + admin approval. Approved stays materialise as owner_stay calendar blocks. Ow"
- `/dashboard/owner-stays/finance-bridge` — "None."
- `/dashboard/payments` — "No real provider integration is active. The manual stub records deposits as `pending`; admins flip t"
- `/dashboard/pricing/channel-push` — "Outbound stub. Records what we WOULD push to a channel manager — no real OTA integration."
- `/dashboard/security` — "Camera registry. The platform never streams video — every camera links out to its vendor app. Owners"
- `/dashboard/service-fulfilment/finance-bridge` — "Bridge completed fulfilments into revenue + expense lines. Idempotent: re-running on a bridged row i"
- `/dashboard/system/health` — "Quick view of migration status, environment readiness, and recent table counts. Failed counts here u"
- `/dashboard/utilities/payments` — "Operator-side payment ledger. Marking a reminder paid optionally creates an expense_line if the peri"
- `/dashboard/utilities/risks` — "Open risks scoped to utility accounts: low/critical balance + no recent reading. Run the unified sca"
- `/development-os` — "Main risks: MEP procurement delay (Surabaya port, 9 days), buyer payment milestone due in 6 days wit"
- `/development-os/ai-agents/inbox` — "No agent outputs are awaiting your review. Trigger an analysis from any agent page, or wait for the "
- `/development-os/ai-agents/memory` — "Memory is created via agent ingestion or manual entry. Run any agent now to seed the first memory it"
- `/development-os/assets` — "Every saleable / rentable / revenue-generating unit in the portfolio. Backed by the `villas` table —"
- `/development-os/cabinets/my-cabinet` — "Main risks: MEP procurement delay (Surabaya port, 9 days), buyer payment milestone due in 6 days wit"
- `/development-os/distributions/new` — "Pick a project, type, and total amount. Preview the per-commitment allocation before declaring. Decl"
- `/development-os/finance/bank-review` — "Daily bookkeeper view: every imported bank transaction with its match + category status. Approve aut"
- `/development-os/finance/document-extractions` — "The AI agent extracts structured data from uploaded receipts, invoices, and delivery notes. Every ex"
- `/development-os/finance/fx` — "Daily FX rates referenced by every transaction's USD conversion. The fx-snapshot cron rolls forward "
- `/development-os/finance/reconciliation` — "Either everything is reconciled or there's nothing to reconcile yet."
- `/development-os/finance/tax-types` — "Operator-configurable tax types. NOT hardcoded; edit rates/applicability/reporting period as needed."
- `/development-os/inventory/stocktake` — "Items at or below their reorder point. Daily cron `dev_os_inventory_low_stock_alert` notifies procur"
- `/development-os/investor-requests` — "Withdrawal / reinvest / transfer / capital-call requests submitted by investors via the portal. Revi"
- `/development-os/profitability` — "Per-asset cost basis + expected margin. `total_cost_basis_minor` and `expected_margin_minor` are GEN"
- `/development-os/revenue-streams` — "Per-period gross + net revenue by asset. `net_revenue_minor` is computed in Postgres as `gross - dir"
- `/development-os/settings` — "Operator-configurable knobs across the Development OS workspace. Each section below opens a dedicate"


