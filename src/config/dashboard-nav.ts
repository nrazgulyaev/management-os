/**
 * @deprecated Moved to `@/config/navigation/management` (Phase 2.0).
 * This shim keeps existing imports working; switch new code to the
 * new path. Will be removed after one release cycle.
 */
export * from "./navigation/management";
